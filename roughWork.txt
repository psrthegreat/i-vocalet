"use strict"

		var query = req.query.q;

		musics.insertEntry(query, function(err, music){
			"use strict"

			if(err) return next(err);

			return res.json(music);
		});	




for (var note in $scope.notes){
			var noteObject = note;
			for (var key in noteObject){
				if(key == chord){
					noteObject[key].forEach(function(chordnote){
						console.log(chordnote);
						Synth.play('piano', NOTES[(chordnote%12)], 4, 2);
					});
				}
			}
		}	